"""Generated by paispl.generated/v1; regenerate instead of editing this file."""

import json
from http.server import BaseHTTPRequestHandler, ThreadingHTTPServer

COMPONENT = {
  "component_id": "CMP-CLINICAL-AI",
  "component_name": "Clinical AI Service",
  "component_type": "service",
  "feature_ids": [
    "clinical_ai_agent",
    "cloud_model"
  ],
  "interface_ids": [
    "API-AI-INFER",
    "EVT-AI-AUDIT"
  ]
}


class Handler(BaseHTTPRequestHandler):
    def do_GET(self):
        if self.path not in {"/health", "/metadata"}:
            self.send_error(404)
            return
        payload = {"status": "ok"} if self.path == "/health" else COMPONENT
        body = json.dumps(payload, ensure_ascii=False).encode("utf-8")
        self.send_response(200)
        self.send_header("Content-Type", "application/json; charset=utf-8")
        self.send_header("Content-Length", str(len(body)))
        self.end_headers()
        self.wfile.write(body)


if __name__ == "__main__":
    ThreadingHTTPServer(("0.0.0.0", 8080), Handler).serve_forever()
